providers = ["node"]

[variables]
NODE_VERSION = "20"
NEXT_TELEMETRY_DISABLED = "1"

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run prisma:generate", "npm run build"]

[start]
cmd = "npm run start:coolify"

